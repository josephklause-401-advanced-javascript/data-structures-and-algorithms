const HashTable = require('../hash-table/hashTable');
const { leftJoin } = require('../leftJoin');

describe('left join', () => {

  it('joins two hashtables using left join logic (all key-values in left hash table are returned, and cooresponding right will be returned', () => {

  });

  it('if there is no cooresponding right value, it will return null', () => {

  });
  
  it('if there is extra right keys they will not be included', ()=> {

  });
});
